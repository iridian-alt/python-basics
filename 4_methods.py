variable.upper() = 'TODOS LOS CARACTERES EN MAYUSCULAS'
variable.capitalize() = 'Solo la priemra en mayuscula'
variable.lower() = 'todos los caracteres en minuscula'
variable.strip() = 'eliminar espacios basura del string'
variable.replace('caracter a cambiar', 'caracter por poner') #reemplaza los caracteres por el que le digamos.
